name: alexandria
description: "Arrange, tag, sort, filter any files"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.1.5
  cupertino_icons: ^1.0.8
  logging: ^1.3.0
  file_picker: 10.3.3
  crypto: 3.0.6
  video_player: ^2.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
